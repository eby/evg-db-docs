= authority.control_set_auth_field_metabib_field_map_blind_refs_only (view)
:description: View reference for authority.control_set_auth_field_metabib_field_map_blind_refs_only

xref:schemas/authority.adoc[↑ authority schema]

[NOTE]
====
This is a *database view*, not a base table. It has no triggers, indexes, or FK constraints of its own. Querying this view may be more efficient than joining the underlying tables directly.
====

metabib fields for NON-main entry auth fields that can't be linked to other records

== Columns

[cols="2,2,1,4"]
|===
|Column |Type |Nullable |Notes

| authority_field
| `integer`
| Yes
| 

| metabib_field
| `integer`
| Yes
| 

|===

== View Definition

[source,sql]
----
 SELECT r.authority_field,
    r.metabib_field
   FROM authority.control_set_auth_field_metabib_field_map_refs_only r
     JOIN authority.control_set_authority_field a ON r.authority_field = a.id
  WHERE a.linking_subfield IS NULL;
----


= action_trigger.event_definition
:description: Table reference for action_trigger.event_definition

xref:schemas/action_trigger.adoc[↑ action_trigger schema]

[CAUTION]
====
*Data-Modifying Triggers:* This table has BEFORE ROW trigger(s) that modify row data before write. Values you INSERT or UPDATE may differ from what is actually stored. See the <<triggers,Triggers>> section below.
====

[WARNING]
====
*Deferrable Constraints:* The following FK constraints are deferrable — they are checked at transaction end, not statement end: `event_definition_cleanup_failure_fkey`, `event_definition_cleanup_success_fkey`, `event_definition_hook_fkey`, `event_definition_opt_in_setting_fkey`, `event_definition_owner_fkey`, `event_definition_reactor_fkey`, `event_definition_validator_fkey`.
====

== Columns

[cols="2,2,1,3,4"]
|===
|Column |Type |Nullable |Default |Notes

| *id* [.label]#PK#
| `integer`
| No
| nextval('action_trigger.event_definition_id_seq'::regclass)
| 

| active
| `boolean`
| No
| true
| 

| owner [.label]#FK#
| `integer`
| No
| 
| → xref:tables/actor/actor_org_unit.adoc[actor.org_unit(id)]

| name
| `text`
| No
| 
| 

| hook [.label]#FK#
| `text`
| No
| 
| → xref:tables/action_trigger/action_trigger_hook.adoc[action_trigger.hook(key)]

| validator [.label]#FK#
| `text`
| No
| 
| → xref:tables/action_trigger/action_trigger_validator.adoc[action_trigger.validator(module)]

| reactor [.label]#FK#
| `text`
| No
| 
| → xref:tables/action_trigger/action_trigger_reactor.adoc[action_trigger.reactor(module)]

| cleanup_success [.label]#FK#
| `text`
| Yes
| 
| → xref:tables/action_trigger/action_trigger_cleanup.adoc[action_trigger.cleanup(module)]

| cleanup_failure [.label]#FK#
| `text`
| Yes
| 
| → xref:tables/action_trigger/action_trigger_cleanup.adoc[action_trigger.cleanup(module)]

| delay
| `interval`
| No
| '00:05:00'::interval
| 

| max_delay
| `interval`
| Yes
| 
| 

| repeat_delay
| `interval`
| Yes
| 
| 

| usr_field
| `text`
| Yes
| 
| 

| opt_in_setting [.label]#FK#
| `text`
| Yes
| 
| → xref:tables/config/config_usr_setting_type.adoc[config.usr_setting_type(name)]

| delay_field
| `text`
| Yes
| 
| 

| group_field
| `text`
| Yes
| 
| 

| template
| `text`
| Yes
| 
| 

| granularity
| `text`
| Yes
| 
| 

| context_usr_path
| `text`
| Yes
| 
| 

| context_library_path
| `text`
| Yes
| 
| 

| context_bib_path
| `text`
| Yes
| 
| 

| context_item_path
| `text`
| Yes
| 
| 

| message_template
| `text`
| Yes
| 
| 

| message_usr_path
| `text`
| Yes
| 
| 

| message_library_path
| `text`
| Yes
| 
| 

| message_title
| `text`
| Yes
| 
| 

| retention_interval
| `interval`
| Yes
| 
| 

|===

== Primary Key

`(id)`

== Foreign Keys

[cols="2,3,1,1,1,2"]
|===
|Column(s) |References |On Delete |On Update |Deferrable |Constraint

| `cleanup_failure`
| xref:tables/action_trigger/action_trigger_cleanup.adoc[action_trigger.cleanup(module)]
| NO ACTION
| NO ACTION
| DEFERRED
| `event_definition_cleanup_failure_fkey`

| `cleanup_success`
| xref:tables/action_trigger/action_trigger_cleanup.adoc[action_trigger.cleanup(module)]
| NO ACTION
| NO ACTION
| DEFERRED
| `event_definition_cleanup_success_fkey`

| `hook`
| xref:tables/action_trigger/action_trigger_hook.adoc[action_trigger.hook(key)]
| NO ACTION
| NO ACTION
| DEFERRED
| `event_definition_hook_fkey`

| `opt_in_setting`
| xref:tables/config/config_usr_setting_type.adoc[config.usr_setting_type(name)]
| NO ACTION
| NO ACTION
| DEFERRED
| `event_definition_opt_in_setting_fkey`

| `owner`
| xref:tables/actor/actor_org_unit.adoc[actor.org_unit(id)]
| NO ACTION
| NO ACTION
| DEFERRED
| `event_definition_owner_fkey`

| `reactor`
| xref:tables/action_trigger/action_trigger_reactor.adoc[action_trigger.reactor(module)]
| NO ACTION
| NO ACTION
| DEFERRED
| `event_definition_reactor_fkey`

| `validator`
| xref:tables/action_trigger/action_trigger_validator.adoc[action_trigger.validator(module)]
| NO ACTION
| NO ACTION
| DEFERRED
| `event_definition_validator_fkey`

|===

== Unique Constraints

* `ev_def_name_owner_once`: `(owner, name)`
* `ev_def_owner_hook_val_react_clean_delay_once`: `(owner, hook, validator, reactor, delay, delay_field)`

== Indexes

[cols="3,1,5"]
|===
|Index |Method |Definition

| `event_definition_pkey` [.label]#PK#
| btree
| `CREATE UNIQUE INDEX event_definition_pkey ON action_trigger.event_definition USING btree (id)`

| `ev_def_name_owner_once` [.label]#UNIQUE#
| btree
| `CREATE UNIQUE INDEX ev_def_name_owner_once ON action_trigger.event_definition USING btree (owner, name)`

| `ev_def_owner_hook_val_react_clean_delay_once` [.label]#UNIQUE#
| btree
| `CREATE UNIQUE INDEX ev_def_owner_hook_val_react_clean_delay_once ON action_trigger.event_definition USING btree (owner, hook, validator, reactor, delay, delay_field)`

|===

[[triggers]]
== Triggers

[cols="3,1,2,1,3"]
|===
|Trigger |Timing |Event |Level |Function

| `is_valid_retention_interval`
| [.label]#BEFORE#
| INSERT OR UPDATE
| ROW
| xref:functions/action_trigger.adoc#check_valid_retention_interval[`action_trigger.check_valid_retention_interval()`]

|===

=== Trigger Bodies

==== is_valid_retention_interval

*Function:* xref:functions/action_trigger.adoc#check_valid_retention_interval[`action_trigger.check_valid_retention_interval()`] +
*Timing:* BEFORE INSERT OR UPDATE ROW

[CAUTION]
====
This trigger modifies the row before it is written (returns a modified `NEW`).
====

[source,plpgsql]
----

BEGIN
    /*
     * 1. Retention intervals are always allowed on active hooks.
     * 2. On passive hooks, retention intervals are only allowed
     *    when the event definition has a max_delay value and the
     *    retention_interval value is greater than the difference 
     *    beteween the delay and max_delay values.
     */ 
    PERFORM TRUE FROM action_trigger.hook 
        WHERE key = NEW.hook AND NOT passive;

    IF FOUND THEN
        RETURN NEW;
    END IF;

    IF NEW.max_delay IS NOT NULL THEN
        IF EXTRACT(EPOCH FROM NEW.retention_interval) > 
            ABS(EXTRACT(EPOCH FROM (NEW.max_delay - NEW.delay))) THEN
            RETURN NEW; -- all good
        ELSE
            RAISE EXCEPTION 'retention_interval is too short';
        END IF;
    ELSE
        RAISE EXCEPTION 'retention_interval requires max_delay';
    END IF;
END;
----

== Referenced By

The following tables have foreign keys pointing to `action_trigger.event_definition` (5 referencing table(s)):

[cols="3,2,2,2"]
|===
|Table |Referencing Column(s) |Referenced Column(s) |Constraint

| xref:tables/action_trigger/action_trigger_alternate_template.adoc[action_trigger.alternate_template]
| `event_def`
| `id`
| `alternate_template_event_def_fkey`

| xref:tables/action_trigger/action_trigger_environment.adoc[action_trigger.environment]
| `event_def`
| `id`
| `environment_event_def_fkey`

| xref:tables/action_trigger/action_trigger_event.adoc[action_trigger.event]
| `event_def`
| `id`
| `event_event_def_fkey`

| xref:tables/action_trigger/action_trigger_event_def_group_member.adoc[action_trigger.event_def_group_member]
| `event_def`
| `id`
| `event_def_group_member_event_def_fkey`

| xref:tables/action_trigger/action_trigger_event_params.adoc[action_trigger.event_params]
| `event_def`
| `id`
| `event_params_event_def_fkey`

|===


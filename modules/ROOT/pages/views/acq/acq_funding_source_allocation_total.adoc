= acq.funding_source_allocation_total (view)
:description: View reference for acq.funding_source_allocation_total

xref:schemas/acq.adoc[↑ acq schema]

[NOTE]
====
This is a *database view*, not a base table. It has no triggers, indexes, or FK constraints of its own. Querying this view may be more efficient than joining the underlying tables directly.
====

== Columns

[cols="2,2,1,4"]
|===
|Column |Type |Nullable |Notes

| funding_source
| `integer`
| Yes
| 

| amount
| `numeric(100,2)`
| Yes
| 

|===

== View Definition

[source,sql]
----
 SELECT a.funding_source,
    sum(a.amount)::numeric(100,2) AS amount
   FROM acq.fund_allocation a
  GROUP BY a.funding_source;
----


= biblio.monograph_part
:description: Table reference for biblio.monograph_part

xref:schemas/biblio.adoc[↑ biblio schema]

[WARNING]
====
*Soft Deletes:* This table uses a `deleted` boolean flag rather than physical row deletion. Ad-hoc queries **must** include `WHERE deleted = false` to exclude logically-deleted rows. The application layer enforces this automatically; direct SQL does not.
====

[CAUTION]
====
*Data-Modifying Triggers:* This table has BEFORE ROW trigger(s) that modify row data before write. Values you INSERT or UPDATE may differ from what is actually stored. See the <<triggers,Triggers>> section below.
====

== Columns

[cols="2,2,1,3,4"]
|===
|Column |Type |Nullable |Default |Notes

| *id* [.label]#PK#
| `integer`
| No
| nextval('biblio.monograph_part_id_seq'::regclass)
| 

| record [.label]#FK#
| `bigint`
| No
| 
| → xref:tables/biblio/biblio_record_entry.adoc[biblio.record_entry(id)]

| label
| `text`
| No
| 
| 

| label_sortkey
| `text`
| No
| 
| 

| deleted [.label.warning]#SOFT-DEL#
| `boolean`
| No
| false
| 

| creator
| `integer`
| No
| 1
| 

| create_date
| `timestamp with time zone`
| No
| now()
| 

| editor
| `integer`
| No
| 1
| 

| edit_date
| `timestamp with time zone`
| No
| now()
| 

|===

== Primary Key

`(id)`

== Foreign Keys

[cols="2,3,1,1,1,2"]
|===
|Column(s) |References |On Delete |On Update |Deferrable |Constraint

| `record`
| xref:tables/biblio/biblio_record_entry.adoc[biblio.record_entry(id)]
| NO ACTION
| NO ACTION
| No
| `monograph_part_record_fkey`

|===

== Indexes

[cols="3,1,5"]
|===
|Index |Method |Definition

| `monograph_part_pkey` [.label]#PK#
| btree
| `CREATE UNIQUE INDEX monograph_part_pkey ON biblio.monograph_part USING btree (id)`

| `record_label_unique_idx` [.label]#UNIQUE#
| btree
| `CREATE UNIQUE INDEX record_label_unique_idx ON biblio.monograph_part USING btree (record, label) WHERE (deleted = false)`

|===

[[triggers]]
== Triggers

[cols="3,1,2,1,3"]
|===
|Trigger |Timing |Event |Level |Function

| `norm_sort_label`
| [.label]#BEFORE#
| INSERT OR UPDATE
| ROW
| xref:functions/biblio.adoc#normalize_biblio_monograph_part_sortkey[`biblio.normalize_biblio_monograph_part_sortkey()`]

|===

=== Trigger Bodies

==== norm_sort_label

*Function:* xref:functions/biblio.adoc#normalize_biblio_monograph_part_sortkey[`biblio.normalize_biblio_monograph_part_sortkey()`] +
*Timing:* BEFORE INSERT OR UPDATE ROW

[CAUTION]
====
This trigger modifies the row before it is written (returns a modified `NEW`).
====

[source,plpgsql]
----

BEGIN
    NEW.label_sortkey := REGEXP_REPLACE(
        evergreen.lpad_number_substrings(
            naco_normalize(NEW.label),
            '0',
            10
        ),
        E'\\s+',
        '',
        'g'
    );
    RETURN NEW;
END;
----

== Referenced By

The following tables have foreign keys pointing to `biblio.monograph_part` (1 referencing table(s)):

[cols="3,2,2,2"]
|===
|Table |Referencing Column(s) |Referenced Column(s) |Constraint

| xref:tables/asset/asset_copy_part_map.adoc[asset.copy_part_map]
| `part`
| `id`
| `copy_part_map_part_fkey`

|===


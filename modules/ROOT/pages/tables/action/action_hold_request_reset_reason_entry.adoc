= action.hold_request_reset_reason_entry
:description: Table reference for action.hold_request_reset_reason_entry

xref:schemas/action.adoc[↑ action schema]

[WARNING]
====
*Cascading Deletes:* Deleting rows from this table will cascade to: xref:tables/action/action_hold_request.adoc[action.hold_request].
====

[WARNING]
====
*Deferrable Constraints:* The following FK constraints are deferrable — they are checked at transaction end, not statement end: `hold_request_reset_reason_entry_hold_fkey`, `hold_request_reset_reason_entry_requestor_fkey`, `hold_request_reset_reason_entry_requestor_workstation_fkey`, `hold_request_reset_reason_entry_reset_reason_fkey`.
====

== Columns

[cols="2,2,1,3,4"]
|===
|Column |Type |Nullable |Default |Notes

| *id* [.label]#PK#
| `integer`
| No
| nextval('action.hold_request_reset_reason_entry_id_seq'::regclass)
| 

| hold [.label]#FK#
| `integer`
| Yes
| 
| → xref:tables/action/action_hold_request.adoc[action.hold_request(id)]

| reset_reason [.label]#FK#
| `integer`
| Yes
| 
| → xref:tables/action/action_hold_request_reset_reason.adoc[action.hold_request_reset_reason(id)]

| note
| `text`
| Yes
| 
| 

| reset_time
| `timestamp with time zone`
| Yes
| 
| 

| previous_copy
| `bigint`
| Yes
| 
| 

| requestor [.label]#FK#
| `integer`
| Yes
| 
| → xref:tables/actor/actor_usr.adoc[actor.usr(id)]

| requestor_workstation [.label]#FK#
| `integer`
| Yes
| 
| → xref:tables/actor/actor_workstation.adoc[actor.workstation(id)]

|===

== Primary Key

`(id)`

== Foreign Keys

[cols="2,3,1,1,1,2"]
|===
|Column(s) |References |On Delete |On Update |Deferrable |Constraint

| `hold`
| xref:tables/action/action_hold_request.adoc[action.hold_request(id)]
| [.label]#CASCADE#
| NO ACTION
| DEFERRED
| `hold_request_reset_reason_entry_hold_fkey`

| `requestor`
| xref:tables/actor/actor_usr.adoc[actor.usr(id)]
| NO ACTION
| NO ACTION
| DEFERRED
| `hold_request_reset_reason_entry_requestor_fkey`

| `requestor_workstation`
| xref:tables/actor/actor_workstation.adoc[actor.workstation(id)]
| NO ACTION
| NO ACTION
| DEFERRED
| `hold_request_reset_reason_entry_requestor_workstation_fkey`

| `reset_reason`
| xref:tables/action/action_hold_request_reset_reason.adoc[action.hold_request_reset_reason(id)]
| NO ACTION
| NO ACTION
| DEFERRED
| `hold_request_reset_reason_entry_reset_reason_fkey`

|===

== Indexes

[cols="3,1,5"]
|===
|Index |Method |Definition

| `hold_request_reset_reason_entry_pkey` [.label]#PK#
| btree
| `CREATE UNIQUE INDEX hold_request_reset_reason_entry_pkey ON action.hold_request_reset_reason_entry USING btree (id)`

| `ahrrre_hold_idx`
| btree
| `CREATE INDEX ahrrre_hold_idx ON action.hold_request_reset_reason_entry USING btree (hold)`

|===

[[triggers]]
== Triggers

[cols="3,1,2,1,3"]
|===
|Trigger |Timing |Event |Level |Function

| `action_hold_request_reset_reason_entry_previous_copy_trig`
| AFTER
| INSERT OR UPDATE
| ROW
| xref:functions/evergreen.adoc#fake_fkey_tgr[`evergreen.fake_fkey_tgr()`]

|===

=== Trigger Bodies

==== action_hold_request_reset_reason_entry_previous_copy_trig

*Function:* xref:functions/evergreen.adoc#fake_fkey_tgr[`evergreen.fake_fkey_tgr()`] +
*Timing:* AFTER INSERT OR UPDATE ROW

[source,plpgsql]
----

DECLARE
    copy_id BIGINT;
BEGIN
    EXECUTE 'SELECT ($1).' || quote_ident(TG_ARGV[0]) INTO copy_id USING NEW;
    IF copy_id IS NOT NULL THEN
        PERFORM * FROM asset.copy WHERE id = copy_id;
        IF NOT FOUND THEN
            RAISE EXCEPTION 'Key (%.%=%) does not exist in asset.copy', TG_TABLE_SCHEMA, TG_TABLE_NAME, copy_id;
        END IF;
    END IF;
    RETURN NULL;
END;
----


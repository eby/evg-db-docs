= authority.control_set_auth_field_metabib_field_map_main (view)
:description: View reference for authority.control_set_auth_field_metabib_field_map_main

xref:schemas/authority.adoc[↑ authority schema]

[NOTE]
====
This is a *database view*, not a base table. It has no triggers, indexes, or FK constraints of its own. Querying this view may be more efficient than joining the underlying tables directly.
====

metabib fields for main entry auth fields

== Columns

[cols="2,2,1,4"]
|===
|Column |Type |Nullable |Notes

| authority_field
| `integer`
| Yes
| 

| metabib_field
| `integer`
| Yes
| 

|===

== View Definition

[source,sql]
----
 SELECT DISTINCT b.authority_field,
    m.metabib_field
   FROM authority.control_set_bib_field_metabib_field_map m
     JOIN authority.control_set_bib_field b ON b.id = m.bib_field;
----


= actor.usr_saved_search
:description: Table reference for actor.usr_saved_search

xref:schemas/actor.adoc[↑ actor schema]

[WARNING]
====
*Cascading Deletes:* Deleting rows from this table will cascade to: xref:tables/actor/actor_usr.adoc[actor.usr].
====

[WARNING]
====
*Deferrable Constraints:* The following FK constraints are deferrable — they are checked at transaction end, not statement end: `usr_saved_search_owner_fkey`.
====

== Columns

[cols="2,2,1,3,4"]
|===
|Column |Type |Nullable |Default |Notes

| *id* [.label]#PK#
| `integer`
| No
| nextval('actor.usr_saved_search_id_seq'::regclass)
| 

| owner [.label]#FK#
| `integer`
| No
| 
| → xref:tables/actor/actor_usr.adoc[actor.usr(id)]

| name
| `text`
| No
| 
| 

| create_date
| `timestamp with time zone`
| No
| now()
| 

| query_text
| `text`
| No
| 
| 

| query_type
| `text`
| No
| 'URL'::text
| 

| target
| `text`
| No
| 
| 

|===

== Primary Key

`(id)`

== Foreign Keys

[cols="2,3,1,1,1,2"]
|===
|Column(s) |References |On Delete |On Update |Deferrable |Constraint

| `owner`
| xref:tables/actor/actor_usr.adoc[actor.usr(id)]
| [.label]#CASCADE#
| NO ACTION
| DEFERRED
| `usr_saved_search_owner_fkey`

|===

== Unique Constraints

* `name_once_per_user`: `(owner, name)`

== Check Constraints

* `valid_query_text`: `CHECK ((query_type = 'URL'::text))`
* `valid_target`: `CHECK ((target = ANY (ARRAY['record'::text, 'metarecord'::text, 'callnumber'::text])))`

== Indexes

[cols="3,1,5"]
|===
|Index |Method |Definition

| `usr_saved_search_pkey` [.label]#PK#
| btree
| `CREATE UNIQUE INDEX usr_saved_search_pkey ON actor.usr_saved_search USING btree (id)`

| `name_once_per_user` [.label]#UNIQUE#
| btree
| `CREATE UNIQUE INDEX name_once_per_user ON actor.usr_saved_search USING btree (owner, name)`

|===


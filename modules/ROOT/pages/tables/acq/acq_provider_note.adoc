= acq.provider_note
:description: Table reference for acq.provider_note

xref:schemas/acq.adoc[↑ acq schema]

[WARNING]
====
*Deferrable Constraints:* The following FK constraints are deferrable — they are checked at transaction end, not statement end: `provider_note_creator_fkey`, `provider_note_editor_fkey`, `provider_note_provider_fkey`.
====

== Columns

[cols="2,2,1,3,4"]
|===
|Column |Type |Nullable |Default |Notes

| *id* [.label]#PK#
| `integer`
| No
| nextval('acq.provider_note_id_seq'::regclass)
| 

| provider [.label]#FK#
| `integer`
| No
| 
| → xref:tables/acq/acq_provider.adoc[acq.provider(id)]

| creator [.label]#FK#
| `integer`
| No
| 
| → xref:tables/actor/actor_usr.adoc[actor.usr(id)]

| editor [.label]#FK#
| `integer`
| No
| 
| → xref:tables/actor/actor_usr.adoc[actor.usr(id)]

| create_time
| `timestamp with time zone`
| No
| now()
| 

| edit_time
| `timestamp with time zone`
| No
| now()
| 

| value
| `text`
| No
| 
| 

|===

== Primary Key

`(id)`

== Foreign Keys

[cols="2,3,1,1,1,2"]
|===
|Column(s) |References |On Delete |On Update |Deferrable |Constraint

| `creator`
| xref:tables/actor/actor_usr.adoc[actor.usr(id)]
| NO ACTION
| NO ACTION
| DEFERRED
| `provider_note_creator_fkey`

| `editor`
| xref:tables/actor/actor_usr.adoc[actor.usr(id)]
| NO ACTION
| NO ACTION
| DEFERRED
| `provider_note_editor_fkey`

| `provider`
| xref:tables/acq/acq_provider.adoc[acq.provider(id)]
| NO ACTION
| NO ACTION
| DEFERRED
| `provider_note_provider_fkey`

|===

== Indexes

[cols="3,1,5"]
|===
|Index |Method |Definition

| `provider_note_pkey` [.label]#PK#
| btree
| `CREATE UNIQUE INDEX provider_note_pkey ON acq.provider_note USING btree (id)`

| `acq_pro_note_creator_idx`
| btree
| `CREATE INDEX acq_pro_note_creator_idx ON acq.provider_note USING btree (creator)`

| `acq_pro_note_editor_idx`
| btree
| `CREATE INDEX acq_pro_note_editor_idx ON acq.provider_note USING btree (editor)`

| `acq_pro_note_pro_idx`
| btree
| `CREATE INDEX acq_pro_note_pro_idx ON acq.provider_note USING btree (provider)`

|===


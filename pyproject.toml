[tool.setuptools]
packages = []

[project]
name = "evg-database"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "antora>=0.0.1",
    "psycopg2-binary>=2.9",
    "python-dotenv>=1.0",
]

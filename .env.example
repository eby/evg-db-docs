# Option 1: full DSN (same format as the MCP postgres tool uses)
DATABASE_URI=postgresql://user:password@localhost:5433/evergreen

# Option 2: individual PG* vars
PGHOST=localhost
PGPORT=5432
PGDATABASE=evergreen
PGUSER=evergreen
PGPASSWORD=

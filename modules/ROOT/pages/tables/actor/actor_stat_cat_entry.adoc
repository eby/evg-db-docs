= actor.stat_cat_entry
:description: Table reference for actor.stat_cat_entry

xref:schemas/actor.adoc[↑ actor schema]


User Statistical Catagory Entries

Local data collected about Users is placed into a Statistical
Catagory.  Each library can create entries into any of its own
stat_cats, its ancestors' stat_cats, or its descendants' stat_cats.


[CAUTION]
====
*Data-Modifying Triggers:* This table has BEFORE ROW trigger(s) that modify row data before write. Values you INSERT or UPDATE may differ from what is actually stored. See the <<triggers,Triggers>> section below.
====

[WARNING]
====
*Cascading Deletes:* Deleting rows from this table will cascade to: xref:tables/actor/actor_org_unit.adoc[actor.org_unit], xref:tables/actor/actor_stat_cat.adoc[actor.stat_cat].
====

[WARNING]
====
*Deferrable Constraints:* The following FK constraints are deferrable — they are checked at transaction end, not statement end: `actor_stat_cat_entry_owner_fkey`, `actor_stat_cat_entry_stat_cat_fkey`.
====

[NOTE]
====
*Trigger Side Effects:* Writing to this table automatically triggers writes to other tables:

* `actor_stat_cat_entry_update_trigger` → UPDATE xref:tables/actor/actor_stat_cat_entry_usr_map.adoc[actor.stat_cat_entry_usr_map]
====

== Columns

[cols="2,2,1,3,4"]
|===
|Column |Type |Nullable |Default |Notes

| *id* [.label]#PK#
| `integer`
| No
| nextval('actor.stat_cat_entry_id_seq'::regclass)
| 

| stat_cat [.label]#FK#
| `integer`
| No
| 
| → xref:tables/actor/actor_stat_cat.adoc[actor.stat_cat(id)]

| owner [.label]#FK#
| `integer`
| No
| 
| → xref:tables/actor/actor_org_unit.adoc[actor.org_unit(id)]

| value
| `text`
| No
| 
| 

|===

== Primary Key

`(id)`

== Foreign Keys

[cols="2,3,1,1,1,2"]
|===
|Column(s) |References |On Delete |On Update |Deferrable |Constraint

| `owner`
| xref:tables/actor/actor_org_unit.adoc[actor.org_unit(id)]
| [.label]#CASCADE#
| NO ACTION
| DEFERRED
| `actor_stat_cat_entry_owner_fkey`

| `stat_cat`
| xref:tables/actor/actor_stat_cat.adoc[actor.stat_cat(id)]
| [.label]#CASCADE#
| NO ACTION
| DEFERRED
| `actor_stat_cat_entry_stat_cat_fkey`

|===

== Unique Constraints

* `sce_once_per_owner`: `(stat_cat, owner, value)`

== Indexes

[cols="3,1,5"]
|===
|Index |Method |Definition

| `stat_cat_entry_pkey` [.label]#PK#
| btree
| `CREATE UNIQUE INDEX stat_cat_entry_pkey ON actor.stat_cat_entry USING btree (id)`

| `sce_once_per_owner` [.label]#UNIQUE#
| btree
| `CREATE UNIQUE INDEX sce_once_per_owner ON actor.stat_cat_entry USING btree (stat_cat, owner, value)`

|===

[[triggers]]
== Triggers

[cols="3,1,2,1,3"]
|===
|Trigger |Timing |Event |Level |Function

| `actor_stat_cat_entry_delete_trigger`
| AFTER
| DELETE
| ROW
| xref:functions/actor.adoc#stat_cat_entry_usr_map_cascade_delete[`actor.stat_cat_entry_usr_map_cascade_delete()`]

| `actor_stat_cat_entry_update_trigger`
| [.label]#BEFORE#
| UPDATE
| ROW
| xref:functions/actor.adoc#stat_cat_entry_usr_map_cascade_update[`actor.stat_cat_entry_usr_map_cascade_update()`]

|===

=== Trigger Bodies

==== actor_stat_cat_entry_delete_trigger

*Function:* xref:functions/actor.adoc#stat_cat_entry_usr_map_cascade_delete[`actor.stat_cat_entry_usr_map_cascade_delete()`] +
*Timing:* AFTER DELETE ROW

[source,plpgsql]
----

BEGIN
    DELETE FROM actor.stat_cat_entry_usr_map
    WHERE stat_cat_entry = OLD.value
        AND stat_cat = OLD.stat_cat;

    RETURN NEW;
END;
----

==== actor_stat_cat_entry_update_trigger

*Function:* xref:functions/actor.adoc#stat_cat_entry_usr_map_cascade_update[`actor.stat_cat_entry_usr_map_cascade_update()`] +
*Timing:* BEFORE UPDATE ROW

[CAUTION]
====
This trigger modifies the row before it is written (returns a modified `NEW`).
====

[source,plpgsql]
----

BEGIN
    UPDATE actor.stat_cat_entry_usr_map
    SET stat_cat_entry = NEW.value
    WHERE stat_cat_entry = OLD.value
        AND stat_cat = OLD.stat_cat;
        
    RETURN NEW;
END;
----

== Referenced By

The following tables have foreign keys pointing to `actor.stat_cat_entry` (1 referencing table(s)):

[cols="3,2,2,2"]
|===
|Table |Referencing Column(s) |Referenced Column(s) |Constraint

| xref:tables/actor/actor_stat_cat_entry_default.adoc[actor.stat_cat_entry_default]
| `stat_cat_entry`
| `id`
| `stat_cat_entry_default_stat_cat_entry_fkey`

|===


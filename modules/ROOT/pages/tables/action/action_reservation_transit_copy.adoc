= action.reservation_transit_copy
:description: Table reference for action.reservation_transit_copy

xref:schemas/action.adoc[↑ action schema]

[WARNING]
====
*Cascading Deletes:* Deleting rows from this table will cascade to: xref:tables/booking/booking_resource.adoc[booking.resource].
====

[WARNING]
====
*Deferrable Constraints:* The following FK constraints are deferrable — they are checked at transaction end, not statement end: `artc_tc_fkey`, `reservation_transit_copy_reservation_fkey`.
====

== Columns

[cols="2,2,1,3,4"]
|===
|Column |Type |Nullable |Default |Notes

| *id* [.label]#PK#
| `integer`
| No
| nextval('action.transit_copy_id_seq'::regclass)
| 

| source_send_time
| `timestamp with time zone`
| Yes
| 
| 

| dest_recv_time
| `timestamp with time zone`
| Yes
| 
| 

| target_copy [.label]#FK#
| `bigint`
| No
| 
| → xref:tables/booking/booking_resource.adoc[booking.resource(id)]

| source
| `integer`
| No
| 
| 

| dest
| `integer`
| No
| 
| 

| prev_hop
| `integer`
| Yes
| 
| 

| copy_status
| `integer`
| No
| 
| 

| persistant_transfer
| `boolean`
| No
| false
| 

| prev_dest
| `integer`
| Yes
| 
| 

| cancel_time
| `timestamp with time zone`
| Yes
| 
| 

| reservation [.label]#FK#
| `integer`
| Yes
| 
| → xref:tables/booking/booking_reservation.adoc[booking.reservation(id)]

|===

== Primary Key

`(id)`

== Foreign Keys

[cols="2,3,1,1,1,2"]
|===
|Column(s) |References |On Delete |On Update |Deferrable |Constraint

| `target_copy`
| xref:tables/booking/booking_resource.adoc[booking.resource(id)]
| [.label]#CASCADE#
| NO ACTION
| DEFERRED
| `artc_tc_fkey`

| `reservation`
| xref:tables/booking/booking_reservation.adoc[booking.reservation(id)]
| SET NULL
| NO ACTION
| DEFERRED
| `reservation_transit_copy_reservation_fkey`

|===

== Indexes

[cols="3,1,5"]
|===
|Index |Method |Definition

| `reservation_transit_copy_pkey` [.label]#PK#
| btree
| `CREATE UNIQUE INDEX reservation_transit_copy_pkey ON action.reservation_transit_copy USING btree (id)`

| `active_reservation_transit_cp_idx`
| btree
| `CREATE INDEX active_reservation_transit_cp_idx ON action.reservation_transit_copy USING btree (target_copy)`

| `active_reservation_transit_dest_idx`
| btree
| `CREATE INDEX active_reservation_transit_dest_idx ON action.reservation_transit_copy USING btree (dest)`

| `active_reservation_transit_source_idx`
| btree
| `CREATE INDEX active_reservation_transit_source_idx ON action.reservation_transit_copy USING btree (source)`

|===

[[triggers]]
== Triggers

[cols="3,1,2,1,3"]
|===
|Trigger |Timing |Event |Level |Function

| `reservation_transit_copy_is_unique_check`
| AFTER
| INSERT
| ROW
| xref:functions/action.adoc#copy_transit_is_unique[`action.copy_transit_is_unique()`]

|===

=== Trigger Bodies

==== reservation_transit_copy_is_unique_check

*Function:* xref:functions/action.adoc#copy_transit_is_unique[`action.copy_transit_is_unique()`] +
*Timing:* AFTER INSERT ROW

[source,plpgsql]
----

BEGIN
    PERFORM * FROM action.transit_copy 
        WHERE target_copy = NEW.target_copy 
              AND dest_recv_time IS NULL 
              AND cancel_time IS NULL;

    IF FOUND THEN
        RAISE EXCEPTION 'Copy id=% is already in transit', NEW.target_copy;
    END IF;
    RETURN NULL;
END;
----

